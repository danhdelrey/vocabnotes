import 'package:flutter/material.dart';

class WritingSetting extends StatelessWidget {
  const WritingSetting({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}